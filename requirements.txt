# Core (both tasks)
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
pandas>=2.0
matplotlib>=3.7
joblib>=1.3

# Task 1 (NLP)
nltk>=3.8            # external movie_reviews evaluation + preprocessing resources:
                    #   python -c "import nltk; [nltk.download(r) for r in ('movie_reviews','stopwords','punkt','wordnet')]"
                    # The BiLSTM also needs GloVe vectors at task1_nlp/data/glove.6B.100d.txt
                    # (https://nlp.stanford.edu/projects/glove/, 6B/100d). Absent, it
                    # falls back to embeddings learned from scratch.

# Report build (make_report.py); the PDF itself is rendered by headless Chrome
markdown>=3.5

# Task 2 (CV)
opencv-python>=4.8   # preprocessing, affine warps
scikit-image>=0.22   # HOG descriptors
torch>=2.0           # ONLY needed for the heatmap CNN; the PCA shape-model
                    # path runs without it
